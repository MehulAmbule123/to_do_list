<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">To-Do List</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" style="display: flex; flex-direction: row; justify-content:space-between;" id="navbarNavAltMarkup">
    <div class="navbar-nav" >
      <a class="nav-item nav-link active" href="#">ToDo</a>
      <a class="nav-item nav-link" href="#">Done</a>
      </div>
      <div class="navbar-nav" style="margin-left: 900px;" >
      <%if !user_signed_in?%>
      <%= link_to "Log IN", new_user_session_path, method: :post, class: "btn btn-outline-success me-2", data: { confirm: "Are you sure?" } %>
      <%= link_to "Sign Up", new_user_registration_path, method: :post, class: "btn btn-outline-success me-2", data: { confirm: "Are you sure?" } %>
      <%else%>
      <%= form_tag(destroy_user_session_path, method: :delete) do %>
      <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-outline-success me-2" %>
      <% end %>
      <a class="nav-item nav-link" href="#"><%=current_user.email%></a>
      <%end%>
      </div>
    <div>
  </div>
</nav>